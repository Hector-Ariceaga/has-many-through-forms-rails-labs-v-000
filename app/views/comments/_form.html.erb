
<%= form_for @post.comments.build do |f| %>
  <%= f.text_field :content, placeholder: "Add your comment here!" %><br>
  <%= f.label :user, "Select your user" %>
  <%= f.collection_select(:user_id, User.all, :id, :username) %><br>
  <%= f.hidden_field :posts_id %>
  <%= f.submit %>
<% end %>
